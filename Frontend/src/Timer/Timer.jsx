function Timer({ timer }) {
  return (
    <div className="flex justify-center">
      <span className="text-2xl font-bold p-3 bg-gray-300 rounded-full">{timer}</span>
    </div>
  );
}

export default Timer;
